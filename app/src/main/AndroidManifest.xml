<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.talentmanagementsystem">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.FeedbackActivity">
        </activity>
        <activity android:name=".ui.activities.ScheduleActivity">
        </activity>
        <activity android:name=".ui.activities.AssignmentActivity">
        </activity>
        <activity android:name=".ui.activities.ChangePasswordActivity">
        </activity>
        <activity android:name=".ui.activities.DetailActivitiesActivity">
        </activity>
        <activity android:name=".ui.activities.ActivitiesActivity">
        </activity>
        <activity android:name=".ui.activities.ProfileActivity">
        </activity>
        <activity android:name=".ui.activities.RegisterActivity">
        </activity>
        <activity android:name=".ui.activities.MainActivity">
        </activity>
        <activity android:name=".ui.activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>